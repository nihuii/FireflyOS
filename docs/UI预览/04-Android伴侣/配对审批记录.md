# 手表配对与解绑预览审批记录

## 2026-07-05 审批稿

- 状态：**已批准**。
- 预览文件：`watch-pairing.html`。
- 本轮只提交视觉与交互方案，不实现 BLE Secure Connections、绑定存储、HMAC 或正式 LVGL 页面。

## 待确认内容

- 配对请求同时显示六位验证码和手机设备名。
- `Allow` 与 `Deny` 均为至少 48px 触摸目标，默认不自动接受。
- 成功态说明手机同步是可选增强，断连不影响手表核心功能。
- 失败态明确没有保存手机数据，并提供重试或取消。
- 解除绑定由手表最终确认；清除 bonding、128-bit app token、通知缓存和手机设备名。
- 解除绑定不删除本地闹钟、媒体和活动数据。

## 审批门

用户已于 2026-07-06 明确回复“批准”。允许按本预览修改 `SystemOverlayHost`、`StorageService` 和 `ConnectivityService`，实现 Task 4 的正式配对、认证与解绑流程；该批准不包含 Task 5、提交或合并授权。
