# Wi-Fi 配网与天气预览审批记录

## 当前状态

`APPROVED`

## 待审批范围

- `配网.html`：Android 选择网络、密码提示、60 秒有效期、等待确认、成功与失败原因；手表确认 SSID、连接进度、失败、已保存网络与忘记网络二次确认。
- `天气.html`：新鲜、3 小时旧化、24 小时较早、无位置、无网络、更新中和服务错误共 7 种状态。
- 天气图标采用最多 12 个 48 × 48 单色/A8 资源；`天气图标母图-v1.png` 已按批准方向生成，但母图本身仍需用户逐图复核，尚未转换或宣称正式图标通过。
- 两端均遵守最小触摸目标；手表使用 410 × 502 画布、24px 基础安全区和 LVGL 8.3.11。

## 审批边界

用户已于 2026-08-01 明确回复“批准”。允许按本组预览继续实现计划 6 Task 2～5 的正式配网与天气界面、服务接线和占位图标资源；本次批准不包含提交、合并、推送或任何真机通过声明。

实现仍须遵守：

- 配网正式 UI 不显示密码明文，破坏性清理必须二次确认；
- WeatherApp 只读取 WeatherSnapshot，不能在 LVGL 回调中执行网络或文件 I/O；
- 不把预览、自动测试或编译结果记录为真机通过；
- 不提交、合并或推送本组文件。
