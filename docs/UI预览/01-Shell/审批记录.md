# UI Shell 审批记录

| 日期 | 状态 | 范围 | 约束 |
|---|---|---|---|
| 2026-07-01 | 已批准执行 | 复用 `image/ui_mockups` 中的一瞥、锁屏、桌面、控制中心、通知中心和应用页面作为计划 2 视觉基准 | 缺失美术先使用占位资源；整理为 MD 后替换；执行途中不打断询问 |

本记录对应用户指令：“开始执行计划 2，执行计划途中需要提供的美术资源整理为 md 文档，这些美术资源先用其他资源占位，之后替换。开始执行，中间不要打断。”

固件实现必须继续遵守 410 × 502 画布、44px 圆角预设、24px 安全区和最小 48 × 48 触摸区。
