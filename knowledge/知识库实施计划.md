# FireflyOS 旧实现参考知识库实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `codex/wifi-weather-ota@e4fb0d1` 中可供重启开发参考的实现，按功能模块提炼为可追溯、可检索、明确风险边界的知识库。

**Architecture:** 以 Markdown 模块卡片承载职责、接口、数据流、精选代码、测试映射和复用建议，以固定 Git 提交代替工作树绝对路径。使用一个只读 Python 校验器检查模块完整性、内部链接、Git 对象引用、风险声明和敏感内容边界。

**Tech Stack:** Markdown、Git 对象数据库、Python 3 标准库、ESP32 Arduino C++、Kotlin/Android 源码引用。

**Execution constraint:** 用户已明确要求直接在当前 `main` 的 `knowledge` 目录实施；本计划不创建分支或工作树，不修改功能代码，不执行提交或推送。

---

### Task 1: 建立总索引和固定来源清单

**Files:**
- Create: `knowledge/README.md`
- Create: `knowledge/00-源码清单与取回方法.md`

- [ ] **Step 1: 写入总索引**

列出 14 个模块、复用等级、当前 `main@351dff7`、来源 `e4fb0d1`、已知高风险问题和推荐阅读顺序。

- [ ] **Step 2: 写入源码取回说明**

提供以下可直接执行的取回方式，并按模块列出源码、测试和文档路径：

```powershell
git show "e4fb0d1:libraries/FireflyOS/src/firefly/services/WifiService.h"
git cat-file -e "e4fb0d1:libraries/FireflyOS/src/firefly/services/WifiService.h"
```

- [ ] **Step 3: 检查来源提交和索引链接**

Run: `git cat-file -e e4fb0d1^{commit}`  
Expected: exit code `0`。

Run: `rg -n "e4fb0d1|351dff7|PENDING" knowledge/README.md knowledge/00-源码清单与取回方法.md`  
Expected: 两个文件均包含来源和验收边界。

### Task 2: 提炼核心运行时和 UI Shell

**Files:**
- Create: `knowledge/01-核心运行时与事件状态.md`
- Create: `knowledge/02-UI-Shell与导航覆盖层.md`

- [ ] **Step 1: 提炼核心运行时**

覆盖 `EventBus`、`StateStore`、`SystemLifecycle`、`AppManager`、`ResourceGovernor`、能力注册和固定容量事件队列；记录后台线程不得访问 LVGL。

- [ ] **Step 2: 提炼 UI Shell**

覆盖 `UiShell`、`NavigationController`、`SystemOverlayHost`、控制中心和通知中心；将下拉面板无法可靠上滑返回标记为“仅作反例或诊断参考”。

- [ ] **Step 3: 验证关键符号映射**

Run: `rg -n "EventBus|StateStore|UiShell|NavigationController|status_drag_cb" knowledge/01-核心运行时与事件状态.md knowledge/02-UI-Shell与导航覆盖层.md`  
Expected: 所有关键符号均能定位到说明和来源。

### Task 3: 提炼时间、电源、触摸和 IMU

**Files:**
- Create: `knowledge/03-时间闹钟与调度.md`
- Create: `knowledge/04-电源按键触摸与IMU.md`

- [ ] **Step 1: 提炼时间和闹钟**

覆盖 `TimeService`、`AlarmService`、RTC/NTP 边界、闹钟音频释放、定时器与秒表状态。

- [ ] **Step 2: 提炼电源、输入和运动**

覆盖 `PowerService`、`InputService`、`MotionService`、`Qmi8658Device`、BOOT 动作和共享 I²C 所有权。

- [ ] **Step 3: 写入触摸竞态反例**

明确 `touch.h` 直接使用 `Wire` 与后台 PMU/IMU 轮询之间的竞态，要求重启实现统一走 `I2cBusManager` 或单一硬件所有者。

- [ ] **Step 4: 验证风险声明**

Run: `rg -n "I2C|Wire|LVGL|BOOT|PENDING" knowledge/03-时间闹钟与调度.md knowledge/04-电源按键触摸与IMU.md`  
Expected: 并发、界面和真机边界均有明确说明。

### Task 4: 提炼存储、音频和主题资源

**Files:**
- Create: `knowledge/05-存储文件与SD卡.md`
- Create: `knowledge/06-音频音乐与录音.md`
- Create: `knowledge/07-主题包与资源管理.md`

- [ ] **Step 1: 提炼存储边界**

覆盖 `SdCardDevice`、`StorageService`、`FileScanService`、受管目录、独占租约、原子写入和拔卡事件。

- [ ] **Step 2: 提炼音频生命周期**

覆盖 `Es8311Device`、`AudioService`、音乐队列、录音 WAV 修复、闹钟抢占和省电策略。

- [ ] **Step 3: 提炼主题包验证**

覆盖 `ThemePackageService`、清单校验、缓存、原子激活、UI 主循环应用和失败回滚。

- [ ] **Step 4: 验证模块所有权**

Run: `rg -n "StorageService|AudioService|ThemePackageService|独占|原子|PENDING" knowledge/05-存储文件与SD卡.md knowledge/06-音频音乐与录音.md knowledge/07-主题包与资源管理.md`  
Expected: 资源所有者和失败路径可检索。

### Task 5: 提炼 BLE、协议和 Android 伴侣

**Files:**
- Create: `knowledge/08-BLE协议配对与通知.md`
- Create: `knowledge/09-Android伴侣应用.md`

- [ ] **Step 1: 提炼手表端 BLE 和协议**

覆盖 `FrameCodec`、`ProtocolTypes`、`ConnectivityService`、`CompanionSyncService`、`NotificationService`、分片、ACK、重放门和认证帧。

- [ ] **Step 2: 提炼 Android 分层**

覆盖 `FireflyGattClient`、`ConnectionRepository`、可靠发送、配对协调、设置持久化、通知、日历、媒体和 UI 权限边界。

- [ ] **Step 3: 写入安全约束**

记录 LE Secure Connections、MITM、bond、敏感帧认证、序列推进顺序和测试密钥不得用于生产。

- [ ] **Step 4: 验证协议两端映射**

Run: `rg -n "FrameCodec|ACK|MTU|认证|测试密钥|PENDING" knowledge/08-BLE协议配对与通知.md knowledge/09-Android伴侣应用.md`  
Expected: 手表端和 Android 端的对应职责完整。

### Task 6: 提炼 Wi-Fi、天气、传输和 OTA

**Files:**
- Create: `knowledge/10-WiFi-NTP与天气.md`
- Create: `knowledge/11-大文件传输.md`
- Create: `knowledge/12-OTA更新与首启回滚.md`

- [ ] **Step 1: 提炼按需 Wi-Fi 和天气缓存**

覆盖 `WifiService`、安全配网、NTP、`WeatherService`、手机/HTTPS 双来源、时效边界和天气图标。

- [ ] **Step 2: 提炼临时大文件传输**

覆盖手表端 `BulkTransferService`、Android `BulkTransfer`、独占 SD 租约、预检元数据、分块确认、取消与临时文件清理。

- [ ] **Step 3: 提炼 OTA 状态机**

覆盖 `UpdateManifest`、`UpdateSources`、`UpdateService`、`UpdateCoordinator`、`EspOtaPlatform`、双 OTA 分区、签名验证和首启回滚。

- [ ] **Step 4: 验证失败闭锁和容量约束**

Run: `rg -n "失败闭锁|签名|回滚|独占|固定容量|PENDING" knowledge/10-WiFi-NTP与天气.md knowledge/11-大文件传输.md knowledge/12-OTA更新与首启回滚.md`  
Expected: 安全边界和真机状态清晰。

### Task 7: 提炼诊断、恢复、发布和集成顺序

**Files:**
- Create: `knowledge/13-诊断恢复出厂与发布闭锁.md`
- Create: `knowledge/14-主程序集成与后续复用顺序.md`

- [ ] **Step 1: 提炼诊断与恢复**

覆盖 `DiagnosticService`、`FactoryResetService`、`HardwareCapabilities`、受管清理范围、两步 SD 确认和 Release 生产公钥闭锁。

- [ ] **Step 2: 提炼主程序装配**

覆盖 `Firefly.ino`、`FireflyApp.h`、`FireflyInteraction.cpp`、`FireflyState.cpp` 的服务所有权和 UI 主循环调度。

- [ ] **Step 3: 制定重启复用顺序**

按 UI Shell 稳定化、时间闹钟、单所有者 I²C、电源、IMU、存储、音频、BLE、Wi-Fi、传输、OTA 的顺序给出每阶段硬件门槛，禁止整批回灌计划 3～6。

- [ ] **Step 4: 验证集成边界**

Run: `rg -n "Firefly\.ino|LVGL|I2C|Gate|PENDING|失败闭锁" knowledge/13-诊断恢复出厂与发布闭锁.md knowledge/14-主程序集成与后续复用顺序.md`  
Expected: 服务装配、硬件门槛和禁止事项完整。

### Task 8: 建立知识库一致性校验

**Files:**
- Create: `knowledge/validate_knowledge.py`
- Modify: `knowledge/README.md`

- [ ] **Step 1: 实现标准库校验器**

校验器读取预期文件清单，检查 Markdown 必备章节、内部链接、固定提交路径、占位符、私钥正文和 `.worktrees` 永久路径依赖；通过 `git cat-file -e` 验证来源对象。

- [ ] **Step 2: 在 README 记录运行方式**

```powershell
python knowledge/validate_knowledge.py
```

Expected: `Knowledge validation passed: 18 Markdown files, 14 modules.`

- [ ] **Step 3: 执行知识库校验**

Run: `python knowledge/validate_knowledge.py`  
Expected: exit code `0`。

- [ ] **Step 4: 检查变更范围**

Run: `git status --short`  
Expected: 原有 `main` 本地变更保持不动；本任务只新增 `knowledge/`。

Run: `git diff --check -- knowledge`  
Expected: exit code `0`。

- [ ] **Step 5: 保持未提交状态**

不运行 `git add`、`git commit` 或 `git push`；将最终文件、验证结果和原有工作树状态报告给用户。
