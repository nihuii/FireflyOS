# Sleep Icon Change Log

## 模块化核心状态

主工程现已接入本地 `FireflyOS` Arduino 库。库内提供有界事件队列、系统状态快照、硬件能力/应用注册、生命周期、资源仲裁和 HAL 接口；现有 UI 页面仍保留在 `Firefly/` 中并渐进迁移。

- UI/LVGL 仍只由 Arduino `loop()` 所在核心调用。
- 后台任务只检测按键与息屏时序，并向容量 16 的 `EventBus` 投递事件。
- `loop()` 统一消费事件后执行页面动作；后台任务不可用时回退为单核轮询。
- `LegacyBoardAdapter` 包裹现有 RTC、PMU 和 CO5300 对象，旧全局对象暂时保留，避免影响当前页面。
- 新 HAL 的 I2C 事务通过带超时的 FreeRTOS mutex 串行化；持锁期间不更新 UI。
- QMI8658 已通过受锁 HAL 接入运动服务；ES8311 通过同一共享 I2C 锁接入独占音频服务。
- `FIREFLY_GATE_A` 周期日志记录内存低水位、事件投递失败数、桌面切换最大耗时，以及运动有效/无效样本、步数和抬腕事件计数。

统一验证入口为：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\verify_all.ps1
```

## UI Shell 阶段状态

- 新增固定层级 `UiShell`：应用、状态栏、系统面板、系统覆盖层。
- 新增深度 6 的有界导航栈和统一 `Screen` 生命周期。
- 桌面改为 `AppRegistry` 驱动的 3 × 2 分页网格，共 11 个入口；时钟、日历、工具、活动和设置已接通，其余入口使用功能占位。
- 控制中心只消费状态快照；通知中心最多显示 3 条摘要并提供空状态。
- 闹钟优先级高于充电提示，关闭闹钟后可恢复底层充电提示。
- 正式图标与中文字体尚未嵌入；计划 3 正式页面暂用现有字体可显示的短英文，替换规则见 `docs/模块说明/02-美术资源规范.md`。

## 计划 3：核心应用、电源与 IMU

- 时间和双闹钟已迁入 `TimeService` / `AlarmService`，RTC 读写继续通过 HAL 和共享 I2C 锁完成。
- 新增 Clock、Calendar、Tools、Activity 四个真实应用页面；倒计时和秒表均为可交互、离开页面仍持续的单会话控制器。
- Tools 提供固定长度计算器和最长 60 秒的 AMOLED 手电筒；手电筒受电量与温度门限制。
- `PowerService` 已实现确定性空闲/充电/低电量/温度保护状态，BOOT 与 AXP2101 PWR 事件统一回到 UI 主循环执行。
- QMI8658 使用 32 项固定环形缓冲，提供本地计步、活动分钟和抬腕事件；活动聚合每 15 分钟或进入息屏前保存。
- LightSleep 的门禁、准备、平台进入、失败恢复和 UI 主循环边界已经实现，但真实 LightSleep 尚未启用。BOOT、PWR、RTC 的真机唤醒矩阵仍为 0/100，当前 ScreenOff 只把显示亮度设为 0。
- 自动验证已经通过 Python 契约、核心测试草图编译、完整固件编译和文档检查；计步精度、抬腕成功率、LightSleep 和续航不能据此视为真机通过。

详细边界与验收状态见 `docs/模块说明/03-时间闹钟电源与活动.md`。

## 计划 4：存储、音频与媒体

- StorageService 管理版本化 NVS、旧设置迁移和 LittleFS 有界缓存。
- StorageService 统一受管 SD 会话并串行化媒体文件 I/O；连续两次客户端 I/O 失败进入拔卡事件流程。
- SD_MMC 固定在 `/FireflyOS/` 七个受管目录内，连续两次会话失败后发布拔卡事件。
- ES8311/I2S 支持内置闹钟铃声、PCM/WAV 流式播放和 16kHz 单声道录音；功放空闲时关闭。
- Files、Music、Recorder、Themes 已替换桌面占位路由，分别采用 32 项分页、128 首索引、2MB 录音门槛和 512KB 图标包上限。
- Files 后台每轮最多扫描 4 项，并通过 `FilesPageReady` 由 UI 主循环接收 32 项固定页。
- 闹钟通过 `AlarmTriggered` 事件驱动覆盖层和 AudioService，不再由时钟回调直接触发提醒 UI。
- 主题包声明 RGB565 宽高并返回具体资源诊断；应用成功后递归重映射现有 LVGL 控件树主题令牌。
- 录音状态在状态栏和一瞥界面持续可见；后台任务仍不访问 LVGL。
- 2026-07-04 补全后的 Python 全集在受控沙箱外 51 项通过；FireflyCoreTests、AudioProbe 和 Firefly 主固件最新编译通过，文档与 diff 检查通过。沙箱内仍可能因测试临时目录 ACL 出现 2 项环境性 `PermissionError`。
- 软件构建通过不能代替 SD 拔插、录放音、长时间循环、主题回滚和触摸延迟的真机验收。

详细边界与待验项目见 `docs/模块说明/05-存储音频与媒体.md`。

## 本次功能

为 FireflyOS 的息屏界面增加了 4 张循环显示的流萤图片，素材来源于 `image/sleep/` 目录下的 4 张 PNG。

同时为锁屏界面增加了壁纸背景，当前使用第 1 张锁屏壁纸图片作为背景，资源格式现已改为原生 `RGB565`。

桌面界面也增加了壁纸背景，当前使用壁纸目录中的第 2 张图片作为桌面壁纸，资源格式同样改为原生 `RGB565`。

设置界面现已改为整屏布局，背景使用壁纸目录中的第 3 张图片，并同样采用原生 `RGB565` 资源。

## 当前行为逻辑

1. 首次进入息屏时显示第 1 张图。
2. 只有在“锁屏界面进入息屏界面”时，才会切换到下一张图。
3. 4 张图片按顺序循环显示，切到最后一张后会回到第 1 张。
4. 如果屏幕已经进入黑屏状态，此时按 `BOOT` 键唤醒回到息屏界面，只会亮屏并恢复当前图片，不会切换到下一张。
5. 锁屏界面可以向上滑入桌面，但桌面界面不能再滑回锁屏。
6. 黑屏唤醒时会先恢复息屏层并立即刷新，再恢复屏幕亮度，避免先闪出锁屏界面。

## 本次界面调整

- 在息屏界面新增了图片控件，用于显示息屏图标。
- 为避免和图标重叠，息屏时间与日期位置做了下移调整。
- 桌面界面改为“壁纸 + 小应用图标”风格，不再使用原来的大卡片入口。
- 桌面按 3 × 2 分页显示 11 个应用入口；当前以 LVGL Symbol 占位，`Clock`、`Calendar`、`Tools`、`Activity` 和 `Settings` 已接通。
- 设置界面从中间弹出卡片改为整屏页面，并使用第 3 张壁纸作为全屏背景。
- 设置页主题色会在启动时从当前设置壁纸中自动采样，卡片、按钮、滑杆和文字配色会跟随壁纸一起更新。
- 闹钟功能从 `Sound` 中独立为单独的 `Alarm` 页面。
- `Alarm` 页面顶部会显示“下一个闹钟还要多少分钟响铃”，无闹钟时会显示提示文案。
- `Alarm` 页面提供两个闹钟卡片位，卡片内显示时间、重复星期、闹钟名称与启用开关。
- 右下角新增 `+` 按钮，可在空位上新增闹钟。
- 新增/编辑闹钟页面提供时间滚轮、铃声、重复日期与闹钟名称输入，整体风格和设置页保持一致。
- 默认亮度与默认音量统一调整为 `50%`。

## 本次性能优化

为优化“锁屏滑入桌面”时的卡顿，本次又做了两项调整：

- 将锁屏壁纸和桌面壁纸从 PNG 原始字节资源改为原生 `RGB565` 静态资源。
- 将设置界面壁纸也改为原生 `RGB565` 静态资源，避免设置页显示时触发 PNG 解码。
- 将 LVGL 图片缓存 `LV_IMG_CACHE_DEF_SIZE` 从 `1` 调整为 `2`。
- 将桌面小应用图标改为“滑动过程中隐藏，进入桌面后再显示”。
- 去掉桌面全屏蒙版。
- 去掉桌面图标阴影。
- 将 LVGL draw buffer 目标高度从 `80` 行提升到 `120` 行，并保留 `80` 行回退兜底。
- 新增最小风险双核方案：UI/LVGL 固定单核，后台任务固定另一核，仅处理 `BOOT` 短按检测与息屏时序判定。

这样做的好处：

- 锁屏和桌面两张全屏壁纸在滑动过程中不再触发 PNG 解码。
- 设置页背景也不再走 PNG 解码链路，打开设置时的运行时负担更低。
- 两张壁纸可以同时留在图片缓存中，减少滑动时反复顶掉缓存、重复处理的情况。
- 锁屏滑入桌面时，不再同时绘制桌面图标层，减少滑动过程中的对象重绘压力。
- 桌面页少了一层全屏透明混合和多组阴影绘制，软件渲染压力更低。
- 提高 draw buffer 后，单次刷新的行数更多，滑动时分块刷屏次数更少。
- `BOOT` 短按检测与自动息屏/黑屏时序不再和 `lv_timer_handler()` 争用同一条主循环。
- 运行时占用更低，更符合“更省资源、更快响应、不明显影响流畅度”的开发原则。

双核方案当前刻意保持保守边界：

- 后台核不直接调用任何 `lv_obj_*` / `lv_label_set_text` / `lv_img_set_src` 等 LVGL 接口。
- 后台核也不直接访问 PMU / RTC 的 I2C 读取逻辑，避免和 UI 核上的状态刷新产生总线竞争。
- 后台核只负责向有界 `EventBus` 投递事件，真正的界面动作仍由 UI 核执行。

Arduino IDE `Tools` 中和双核相关的两项，当前建议也补充说明如下：

- `Arduino Runs On` 保持当前默认即可。在这套工程里，`setup()` / `loop()` / `lv_timer_handler()` 所在的 UI 主链路跑在哪个核心，后台任务就会自动固定到另一个核心。
- 如果当前板卡包的默认值是 `Core 0`，那么实际效果就是：UI / LVGL 在 `Core 0`，后台任务在 `Core 1`。
- `Events Run On` 当前也保持默认即可。它控制的是 Arduino 事件任务，不是这里手动创建的后台 FreeRTOS 任务。
- 由于当前工程没有把 Arduino 事件回调作为主要性能瓶颈，而且为了降低跨核 UI 调用风险，`Events Run On` 不需要为了这次双核优化单独改动。

## 原因分析

上一版实现为了追求更低运行时开销，使用了自定义的离线图片处理链路：

- 先把 PNG 缩放到 200x200。
- 再手动转换成 `RGB565 + Alpha` 的 LVGL 内置图片格式。

这条链路虽然运行时更省，但颜色处理不是走 LVGL 官方 PNG 解码流程，会受到自定义缩放、颜色量化和 Alpha 处理的影响，所以最终屏幕上有一部分颜色和原图不一致。

## 当前实现方式

现在改为：

- 先把 4 张图片预缩放为 200x200 的 PNG。
- 直接把 PNG 原始字节嵌入 `SleepIcons.cpp`。
- 显示时交给 LVGL 自带的 PNG 解码器处理颜色转换。
- 锁屏壁纸与桌面壁纸则单独使用原生 `RGB565` 静态资源，不再走 PNG 解码链路。
- 设置界面壁纸同样使用原生 `RGB565` 静态资源。
- 设置页主题不再手写固定颜色，而是在启动时从设置壁纸像素中自动采样生成。
- 闹钟列表与闹钟编辑页复用设置页主题色，跟随壁纸自动适配。

这样做的好处：

- 颜色显示和 PNG 本身保持一致，不再依赖手写像素转换逻辑。
- 仍然保留 200x200 资源尺寸，避免直接解码 510x510 原图带来更高内存与耗时。
- 图片只在切换时才需要重新解码，日常息屏显示仍然比较轻量。
- 相比上一版，资源体积也从约 2.96MB 降到了约 1.45MB。

## 涉及文件

- `Firefly/LockWallpaper.h`
  - 锁屏壁纸资源声明。

- `Firefly/LockWallpaper.cpp`
  - 锁屏壁纸与桌面壁纸原生 `RGB565` 资源。

- `Firefly/SettingsWallpaper.h`
  - 设置界面壁纸资源声明。

- `Firefly/SettingsWallpaper.cpp`
  - 设置界面第 3 张壁纸的原生 `RGB565` 资源。

- `Firefly/SleepIcons.h`
  - 息屏图片资源声明与获取接口。

- `Firefly/SleepIcons.cpp`
  - 4 张息屏图标的 PNG 原始字节资源。

- `Firefly/FireflyApp.h`
  - 息屏图片控件声明，并引入息屏图片头文件。

- `Firefly/FireflyState.cpp`
  - 息屏图片控件全局对象定义。

- `Firefly/FireflyInteraction.cpp`
  - 息屏图片索引状态。
  - 息屏图片刷新逻辑。
  - “锁屏进入息屏时切下一张图”的逻辑放在 `enter_sleep_screen_mode()` 中。
  - `wake_sleep_screen_from_blackout()` 只负责黑屏唤醒，不切图。
  - `tileview` 滑动开始时隐藏桌面图标层，滑动结束并真正进入桌面后再显示。
  - 新增固定到另一核心的后台任务。
  - 后台任务只负责 `BOOT` 短按事件与自动息屏/黑屏时序检测。
  - UI 核通过 `EventBus` 消费后台任务结果，保持 LVGL 单核访问。

- `Firefly/Firefly.ino`
  - 在息屏页面中加入图片控件。
  - 调整息屏时间和日期文字位置。
  - 在锁屏页面中加入壁纸图片控件。
  - 桌面页面改为壁纸加图标布局。
  - 设置页面改为整屏壁纸布局。
  - 将 `Sound` 和 `Alarm` 拆分为两个独立页面。
  - 新增双闹钟卡片视图与闹钟新增/编辑页面。
  - 禁止从桌面反向滑回锁屏。
  - 桌面图标改为独立图层，便于在滑动过程中整体隐藏。
  - 去掉桌面全屏蒙版与图标阴影。
  - 将 draw buffer 目标从 `80` 行提升到 `120` 行，并在内存不足时回退到 `80` 行。
  - 在 `setup()` 中启动后台核心任务。

- `Firefly/FireflyTheme.cpp`
  - 从设置壁纸像素中自动采样颜色，并生成设置页主题色。

- `Firefly/FireflyAlarm.h`
  - 双闹钟数据结构、铃声/重复日期选项与下一个闹钟计算接口。

- `Firefly/FireflyAlarm.cpp`
  - 双闹钟的默认值、标签映射与下一次响铃时间计算逻辑。

- `libraries/lv_conf.h`
  - 将 LVGL 图片缓存数量从 `1` 调整为 `2`。

## 备注

- 当前切图不是定时动画，而是按状态切换，因此系统负载更低。
- 当前方案在保证颜色正确性的前提下，仍然尽量遵守“更省资源、更快响应、不明显影响流畅度”的开发原则。
